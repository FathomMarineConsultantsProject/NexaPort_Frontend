import { money, shown } from "./formatters";

export function DataRow({label,value}) { return <div className="workflow-data-row"><span>{label}</span><strong>{shown(value)}</strong></div>; }
export function QuoteBreakdown({quote,includeMarkup=false}) { if(!quote)return null; return <dl className="quote-breakdown"><div><dt>Service / base fee</dt><dd>{money(quote.baseFeeUsd)}</dd></div><div><dt>Travel</dt><dd>{money(quote.travelCostUsd)}</dd></div><div><dt>Accommodation</dt><dd>{money(quote.accommodationCostUsd)}</dd></div><div><dt>Report fee</dt><dd>{money(quote.reportFeeUsd)}</dd></div><div><dt>Urgency</dt><dd>{money(quote.urgencySurchargeUsd)}</dd></div><div className="total"><dt>Consultant total</dt><dd>{money(quote.consultantTotalUsd)}</dd></div>{includeMarkup&&<><div><dt>Nexaport / Admin quotation fee</dt><dd>{money(quote.adminMarkupUsd)}</dd></div><div className="grand-total"><dt>Final client-facing total</dt><dd>{money(quote.clientTotalUsd)}</dd></div></>}</dl>; }
